# Keycloak Server Configuration
# ==========================

# Base URL of your Keycloak server (without trailing slash)
# Example: https://keycloak.example.com or http://localhost:8080
KEYCLOAK_SERVER_URL=https://your-keycloak-server.com

# Name of the Keycloak realm to use
# This is the realm where your applications and users are configured
KEYCLOAK_REALM=your-realm-name


# Default Client Credentials (Optional)
# ====================================

# Default client ID to use for authentication requests
# This can be overridden in individual API calls
KEYCLOAK_CLIENT_ID=your-default-client-id

# Default client secret for confidential clients
# Leave empty for public clients
KEYCLOAK_CLIENT_SECRET=your-client-secret


# Admin Credentials (Optional - For Management Operations)
# =======================================================

# Username with admin privileges for realm management
# Used for creating users, clients, roles, etc.
KEYCLOAK_ADMIN_USERNAME=admin

# Password for the admin user
# In production, consider using service accounts with client credentials
KEYCLOAK_ADMIN_PASSWORD=admin-password


# Additional Configuration (Optional)
# ==================================

# Default timeout for HTTP requests (seconds)
# KEYCLOAK_TIMEOUT=30

# Whether to verify SSL certificates (true/false)
# Set to false only for development with self-signed certificates
# KEYCLOAK_VERIFY_SSL=true

# Default scopes to request (space-separated)
# KEYCLOAK_DEFAULT_SCOPES=openid profile email


# Example Configuration for Local Development
# ==========================================
# Uncomment and modify these for local Keycloak development

# KEYCLOAK_SERVER_URL=http://localhost:8080
# KEYCLOAK_REALM=master
# KEYCLOAK_CLIENT_ID=admin-cli
# KEYCLOAK_ADMIN_USERNAME=admin
# KEYCLOAK_ADMIN_PASSWORD=admin


# Example Configuration for Production
# ===================================
# Example settings for a production environment

# KEYCLOAK_SERVER_URL=https://auth.yourcompany.com
# KEYCLOAK_REALM=production
# KEYCLOAK_CLIENT_ID=backend-service
# KEYCLOAK_CLIENT_SECRET=super-secret-production-key
# KEYCLOAK_VERIFY_SSL=true
# KEYCLOAK_TIMEOUT=30